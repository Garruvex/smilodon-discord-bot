import type { SettingsTextCatalog } from "./catalog.js";

// English text for every registered setting — the source every other
// language falls back to, field by field. One spread per settings group.
export const enSettingsText: SettingsTextCatalog = {};
